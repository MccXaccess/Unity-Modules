using UnityEngine;

public class ModuleLoadScriptableObjects : MonoBehaviour
{
    private void Start()
    {
        /*
        var objects = Resources.LoadAll<BaseCharacterControllerConfiguration>("");

        foreach (var sObject in objects)
        {
            Debug.Log(sObject.value);
        }
        */
    }
}